/*
Task: Write a function void findAnagrams(string s, string p) that prints 
the starting index of every substring in s that is an anagram of p. An anagram contains 
the exact same characters as p with the exact same frequencies, but in any order.
Example: s = "cbaebabacd", p = "abc". Output should print 0 (for "cba") and 6 (for "bac").

*/
#include <iostream>
#include <string>
using namespace std;
void findAnagrams(string s, string p){
    

}